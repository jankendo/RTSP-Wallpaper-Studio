<Application x:Class="RTSPWallpaperStudio.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="WindowBackground" Color="#F5F7FB" />
        <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF" />
        <SolidColorBrush x:Key="Ink" Color="#1D2433" />
        <SolidColorBrush x:Key="MutedInk" Color="#657087" />
        <SolidColorBrush x:Key="Accent" Color="#2764D8" />
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Ink}" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="FontSize" Value="14" />
        </Style>
    </Application.Resources>
</Application>
